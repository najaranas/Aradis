import { initializeApp } from "@react-native-firebase/app";

// The Firebase config is automatically read from google-services.json
// No need to manually specify config values
const app = initializeApp();

export default app;
